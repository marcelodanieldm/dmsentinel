# DM Sentinel v3.0 - Enterprise Security Audit Platform
# Python 3.9+ Required
# Installation: pip install -r requirements.txt

# ==================== CORE DEPENDENCIES ====================

# HTTP Requests & Web Scanning
requests>=2.31.0

# Version Comparison
packaging>=23.0

# DNS Resolution & Email Security Auditing
dnspython>=2.4.0

# HTML Parsing for Form Analysis
beautifulsoup4>=4.12.0

# ==================== WEB FRAMEWORK ====================

# Flask (API REST, Webhooks, Automation Engine)
Flask>=3.0.0
Werkzeug>=3.0.0

# ==================== PAYMENT INTEGRATION ====================

# Stripe - Payment processing and webhook verification
stripe>=7.0.0

# ==================== TELEGRAM INTEGRATION ====================

# Telegram Bot API (for alerts in automation engine)
python-telegram-bot>=20.0  # Optional - Advanced bot features
# Note: For basic sendMessage, only requests library needed (already included)

# ==================== GOOGLE SHEETS CRM & LOGGING ====================

# Google Sheets API for CRM and audit logging (Sprint 3)
gspread>=6.0.0
google-auth>=2.23.0
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.1.1

# Note: oauth2client is deprecated, use google-auth instead

# ==================== PDF REPORT GENERATION ====================

# ReportLab - Professional PDF generation with charts
reportlab>=4.0.4

# Alternative: WeasyPrint (HTML to PDF)
# weasyprint>=59.0
# Note: WeasyPrint requires system dependencies (GTK, Pango, Cairo)

# ==================== DATABASE & PERSISTENCE ====================

# SQLite (included in Python stdlib, no installation needed)
# Used for historical tracking system

# ==================== CONCURRENCY & THREADING ====================

# concurrent.futures (included in Python stdlib, no installation needed)
# Used for multi-target scanning

# ==================== OPTIONAL: TELEGRAM INTEGRATION ====================

# Telegram Bot API (for alerts in automation engine)
python-telegram-bot>=20.0  # Optional

# ==================== DEVELOPMENT & TESTING ====================

# Testing framework (optional)
# pytest>=7.4.0
# pytest-cov>=4.1.0

# Code quality (optional)
# pylint>=3.0.0
# black>=23.0.0

# ==================== PRODUCTION DEPLOYMENT ====================

# WSGI Server for production (optional)
# gunicorn>=21.2.0  # Linux/Mac
# waitress>=2.1.2   # Windows-compatible

# ==================== NOTES ====================

# Minimal installation (core features only):
#   pip install requests packaging dnspython beautifulsoup4 Flask
#
# Full installation (all v3.0 features):
#   pip install -r requirements.txt
#
# Google Sheets setup:
#   1. Create service account at: https://console.cloud.google.com/
#   2. Download credentials.json
#   3. Enable Google Sheets API and Google Drive API
#   4. Share spreadsheet with service account email
#
# PDF generation requirements:
#   - ReportLab: Pure Python, works on all platforms
#   - WeasyPrint: Requires GTK libraries (complex on Windows)
#
# For Make.com integration:
#   - Use REST API endpoints (/api/v3/scan, /api/v3/report)
#   - Generate API key with sentinel_api.generate_api_key()
#   - Include X-API-Key header in all requests
